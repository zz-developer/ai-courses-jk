{
  "name": "ai-courses",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm exec playwright install-deps && pnpm exec playwright install && pnpm -r --filter=./courses/** run build --download && node ./scripts/build.js",
    "build:slides": "pnpm -r --filter=./courses/** run build --download"
  },
  "dependencies": {
    "@slidev/cli": "catalog:",
    "@slidev/theme-default": "catalog:",
    "@slidev/theme-seriph": "catalog:",
    "@slidev/types": "51.6.0",
    "node-tikzjax": "catalog:",
    "slidev-addon-python-runner": "^0.1.3",
    "vue": "catalog:"
  },
  "devDependencies": {
    "playwright-chromium": "^1.52.0"
  }
}
