{
  "name": "ai-courses",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter=./courses/** run build && node ./scripts/build.js",
    "build:slides": "pnpm -r --filter=./courses/** run build"
  },
  "dependencies": {
    "@slidev/cli": "catalog:",
    "@slidev/theme-default": "catalog:",
    "@slidev/theme-seriph": "catalog:",
    "@slidev/types": "51.6.0",
    "node-tikzjax": "catalog:",
    "slidev-addon-python-runner": "^0.1.3",
    "vue": "catalog:"
  },
  "devDependencies": {
    "playwright-chromium": "^1.52.0"
  }
}
